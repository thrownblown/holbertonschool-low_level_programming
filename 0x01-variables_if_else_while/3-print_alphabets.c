#include <stdio.h>

/**
 * main - Entry point
 *  prints the sizes of various data types
 * Return: Always 0 (Success)
 */
int main(void)

{
	int a = 2;
	int b = 1;


	a = 0;
	while (a < b)
		printf("%d\n", a++);
	return (0);
}
