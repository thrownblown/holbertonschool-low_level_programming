#include <stdio.h>

/**
 * main - prints the numbers 0 and above and below 10
 * Return: Always 0 (Success)
 */
int main(void)

{
	int i = 0;

	while (i < 10)
	putchar(i++);

	putchar('\n');
	return (0);
}
